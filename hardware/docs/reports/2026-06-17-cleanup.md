# Hardware Directory Cleanup Log - 2026-06-17

## Scope

Cleanup was restricted to:

- `/home/kjm26/project/PRJXR/XR-VIT/HGTXR/hardware`

Parent `../docs` and sibling `../software` changes were intentionally not
modified by this cleanup.

## Prompt Brief

- Goal: reduce root-level clutter and establish the target hardware directory
  taxonomy without breaking current HLS, PYNQ, reference, or signoff flows.
- Inputs: current file tree, `git status --short`, direct path-reference search,
  existing HGTXR tracking docs.
- Constraints: preserve user changes; avoid destructive cleanup; keep
  compatibility paths used by tests and tools.
- Expected output: normalized directories, moved root logs, cleanup policy docs,
  and validation evidence.
- Acceptance: root no longer contains Vivado/HLS logs or accidental empty files;
  key compatibility paths remain intact; documentation records deferred moves.

## Agent Task Cards

```yaml
task_card:
  task_id: T-001
  sub_agent: "gpt5.3-codex-spark"
  role: "evaluator"
  objective: "Inspect path references and classify cleanup moves as safe or risky."
  file_ownership: []
  assigned_skill:
    - "codebase-cleanup-refactor-clean"
    - "agent-hierarchy-runtime-manager"
  inputs:
    - "hardware tree"
    - "path reference search"
  outputs:
    - "risk table"
  validation:
    - "rg/find evidence only"
  dependencies: []
```

Spark execution failed because the runtime usage limit was reached.

```yaml
task_card:
  task_id: T-001-Retry
  sub_agent: "gpt5.5"
  role: "evaluator"
  objective: "Retry reference-risk classification after Spark limit."
  file_ownership: []
  assigned_skill:
    - "codebase-cleanup-refactor-clean"
    - "agent-hierarchy-runtime-manager"
  inputs:
    - "hardware tree"
    - "path reference search"
  outputs:
    - "risk table"
  validation:
    - "rg/find evidence only"
  dependencies: []
```

## Execution DAG

1. Inventory current status and top-level tree.
2. Identify direct references to planned migration paths.
3. Create target directory taxonomy.
4. Move only root-level generated logs and accidental empty files.
5. Keep direct-reference compatibility paths in place.
6. Validate tree and syntax-sensitive files.

## Completed Moves

| From | To | Reason |
|---|---|---|
| `vitis_hls.log` | `generated/logs/hls/vitis_hls.log` | HLS generated log |
| `vivado.log` | `generated/logs/vivado/vivado.log` | Vivado generated log |
| `vivado.jou` | `generated/logs/vivado/vivado.jou` | Vivado journal |
| `vivado_42.backup.log` | `generated/logs/vivado/vivado_42.backup.log` | Vivado backup log |
| `vivado_42.backup.jou` | `generated/logs/vivado/vivado_42.backup.jou` | Vivado backup journal |
| `=318` | `archive/migration-logs/=318.empty` | Empty accidental root file |
| `=332` | `archive/migration-logs/=332.empty` | Empty accidental root file |

## Risk Decisions

- `hls/`: kept in place because Vivado scripts, docs, static validation, and
  tests use direct paths.
- `pynq/`: kept in place because bundle packaging, smoke import, signoff, and
  validation tools use direct paths.
- `refs/`: kept in place because packaging, test fixtures, static validation,
  and golden-vector tools use direct paths.
- `analysis/vit-accel/`: kept in place because source-audit and validation docs
  use direct paths.
- `generated/pynq/` and `generated/signoff/`: kept in place because final
  signoff evidence and smoke bundles use these exact paths.

## Remaining Work

- Update tooling to support `src/hls`, `src/host/pynq`, and artifact paths.
- Add compatibility symlinks or path resolver helpers before moving heavily
  referenced directories.
- Define `.gitignore` policy for `generated/logs`, `.Xil`, and temporary build
  outputs after reviewing which signoff evidence must stay tracked.
