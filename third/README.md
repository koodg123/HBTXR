# Third-Party References

This directory consolidates third-party repositories and tools referenced by the
three original HBTXR workspaces.

Nested `.git` metadata is excluded from the reorganized workspace. Treat these
directories as vendored references unless a specific upstream sync policy is
defined later.
