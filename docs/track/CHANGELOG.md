> **작성** 2026-07-14 · **갱신** 2026-07-23
> **상태** active
> **소유** repo

# Changelog

## Unreleased

- Added a durable plan for HBTXR-only checkpoint commits.
- Added a multi-agent HANDOVER integration-analysis specification.
- Added validation, execution, progress, decision, and handoff tracking.
- Archived FECET and SWIFT porting sources without activating them.
- Added compact EV-Eye/EX-Gaze analysis and accelerator evidence checkpoints.
- Added six HANDOVER audits covering inventory, software, hardware,
  license/artifact risk, integration decisions, and recommendations.
- Added a concrete gated follow-up plan with T-000–T-630 Task Cards for
  selective software, experiment, hardware, and repository-governance work.
- Added a baseline function/class/line semantic census with machine-readable
  file, symbol, import, duplicate, dead-code-candidate, and line-finding inventories.
- Added a staged repair/refactor/manifest-first cleanup plan with explicit
  compatibility, verification, rollback, and retirement gates.
- Revised the semantic refactor plan to preserve the algorithm modality layout, limit default namespace migration to Quantization, make Hybrid migration conditional/NO-OP by default, and retain `references/**` permanently for comparison experiments.
- Revised the semantic refactor plan with gated RC-120/RC-130 tasks to retire active FACET names and remove `src.pools`, while preserving `EvEye`, modality/reference identity, and `src.optim.pool`.
- Removed proposed `HBTXR_*` replacement environment names from RC-120 in favor of existing config keys and a neutral standalone CLI flag.
- Added the modular algorithm ownership plan with preserved evidence/package
  roots, staged `eveye.*` shared owners, explicit frame/event/Hybrid boundaries,
  and a current-to-target code tree.
- Revised pools retirement to retain loss, optimizer, LR scheduler and runtime
  scheduler APIs in domain packages while preserving `optim/pool.py`.
- Passed independent T-727 review after making package verification environments
  executable, enforcing dependency direction, and ordering demo extraction before
  source moves.

## 2026-07-16

- Added an evidence-backed HANDOVER and continuation prompt for the uncommitted semantic census, standalone selective-integration plan, and revised refactor/cleanup plan.
- Recorded the current branch/HEAD, dirty documentation and recon inventory, sparse worktree, user decisions, remaining gates, and exact restart command without changing production/reference source or creating a commit.
