extends: base.yaml

training:
  stage: stage2
  epochs: 30
  batch_size: 8
  best_metric_name: metric_track_p10_pct
  scheduler:
    type: plateau
    metric_name: metric_track_p10_pct
    factor: 0.5
    patience: 4
  early_stopping:
    enabled: true
    patience: 10
    start_epoch: 6

experiment:
  name: swift_stage2
