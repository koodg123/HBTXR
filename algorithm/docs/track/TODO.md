# TODO

- Normalize hard-coded absolute paths in configs.
- Split training, evaluation, and analysis dependencies into pinned files.
- Add smoke tests for config load, model construction, dataset construction, and
  target export.
- Decide whether large local artifacts belong in Git LFS or external storage.
