# Preset: mode0 + stage1 dense mask-center guidance with axis term

extends: mode0_stage1_dense_maskcenter_guidance.yaml

experiment:
  name: hbtxr_mode0_stage1_dense_maskcenter_guidance_ab50

loss:
  search_mask_center_weight: 0.1
  search_mask_ab_weight: 0.05
  search_mask_guidance_detach: true
