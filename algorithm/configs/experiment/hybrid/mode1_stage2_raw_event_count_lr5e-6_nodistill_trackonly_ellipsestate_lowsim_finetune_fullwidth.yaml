# Stage2 track-only fine-tune with ellipse-state loss plus low-similarity emphasis.
# Disabled-by-default code path becomes active only through the keys below.
# Motivation: XR-04 failure buckets show similarity_target <= 0.1 dominates tail error.

extends: mode1_stage2_raw_event_count_lr5e-6_nodistill_trackonly_ellipsestate_finetune_fullwidth.yaml

loss:
  track_axis_log_weight: 0.05
  track_angle_cos_weight: 0.02
  track_low_similarity_threshold: 0.3
  track_low_similarity_weight_scale: 1.0
  track_low_similarity_max_multiplier: 2.0

experiment:
  name: raw_mode1_stage2_lr5e-6_nodistill_trackonly_ellipsestate_lowsim_finetune_fullwidth
