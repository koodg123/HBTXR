# Stage2 track-only fine-tune with direct decoded-center L2 supervision.
# This keeps the current best fixed-count surface and aligns loss with
# metric_track_center_px more directly than residual smooth-L1 alone.

extends: mode1_stage2_raw_event_count_lr5e-6_nodistill_trackonly_centerloss_adaptivecount_finetune_fullwidth.yaml

loss:
  track_center_l2_weight: 0.0025

experiment:
  name: raw_mode1_stage2_lr5e-6_nodistill_trackonly_centerl2_finetune_fullwidth
