# Stage2 fine-tune: conservative LR sibling of the LR 2e-5 run, started from
# the current centerloss best-center checkpoint.

extends: mode1_stage2_raw_event_count_lr2e-5_weakdistill_centerloss_finetune_fullwidth.yaml

training:
  lr: 1.0e-5
  optimizer:
    name: adamw
    lr: 1.0e-5
    weight_decay: 5.0e-5
    betas: [0.9, 0.999]
    eps: 1.0e-8
    kwargs: {}

experiment:
  name: raw_mode1_stage2_lr1e-5_weakdistill_centerloss_finetune_fullwidth
