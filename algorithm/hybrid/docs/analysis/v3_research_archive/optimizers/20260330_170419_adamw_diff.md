알고리즘 수준 차이 없음.

- 상류 소스 경로/URL:
  - `torch.optim.AdamW`
- HBTXR 반영 경로:
  - `src/hbtxr/optim/adamw.py`
- 동일하게 유지된 부분:
  - update rule, bias correction, state layout, parameter-group semantics는 PyTorch builtin `AdamW`를 그대로 사용합니다.
- 변경한 부분:
  - config-driven registry에서 builtin optimizer를 생성할 수 있도록 thin builder만 추가했습니다.
- benchmark 해석 메모:
  - optimizer pool 비교에서 baseline 기준 optimizer입니다.
