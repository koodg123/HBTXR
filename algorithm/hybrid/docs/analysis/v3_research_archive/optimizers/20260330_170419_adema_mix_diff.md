알고리즘 수준 차이 없음.

- 상류 소스 경로/URL:
  - `https://raw.githubusercontent.com/apple/ml-ademamix/main/pytorch/ademamix.py`
- HBTXR 반영 경로:
  - `src/hbtxr/optim/adema_mix.py`
- 동일하게 유지된 부분:
  - two-EMA update rule, warmup scheduler, bias-corrected denominator, decoupled weight decay update는 공식 PyTorch 구현과 동일합니다.
- 변경한 부분:
  - 현재 공용 optimizer surface가 `betas`를 2개 튜플로 유지하기 때문에, 세 번째 beta는 `training.optimizer.kwargs.beta3`로 노출했습니다.
- benchmark 해석 메모:
  - `beta3`, `alpha`, warmup 설정을 동일하게 주면 공식 AdEMAMix PyTorch 경로와 수치적으로 맞아야 합니다.
