알고리즘 수준 차이 없음.

- 상류 소스 경로/URL:
  - `https://raw.githubusercontent.com/huggingface/pytorch-image-models/main/timm/optim/adopt.py`
- HBTXR 반영 경로:
  - `src/hbtxr/optim/adopt.py`
- 동일하게 유지된 부분:
  - first-step variance bootstrap, normalized-gradient update, clipping, optional decoupled decay, caution path를 유지했습니다.
- 변경한 부분:
  - 현재 trainer wiring에서 쓰지 않는 foreach/capturable/differentiable surface를 제거하고 single-tensor path만 남겼습니다.
- benchmark 해석 메모:
  - single-tensor ADOPT 규칙 자체는 같지만, upstream foreach 최적화 구현과는 throughput이 다를 수 있습니다.
