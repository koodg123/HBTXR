알고리즘 수준 차이 없음.

- 상류 소스 경로/URL:
  - `https://raw.githubusercontent.com/team-approx-bayes/ivon/refs/heads/main/ivon/_ivon.py`
- HBTXR 반영 경로:
  - `src/hbtxr/optim/ivon.py`
- 동일하게 유지된 부분:
  - optimizer update rule, sampled-parameter context, Hessian approximation 옵션, core state transition은 공식 `_ivon.py`와 맞춰 유지했습니다.
- 변경한 부분:
  - 구현을 HBTXR의 `src/hbtxr/optim/<name>.py` flat-file 정책에 맞게 재배치했습니다.
  - registry/config 연동을 위해 `build_ivon_optimizer(...)` helper를 추가했습니다.
  - 사용자가 `training.optimizer.kwargs.ess`를 주지 않으면 trainer가 `len(train_loader.dataset)`를 자동 주입합니다.
  - `sampled_params`를 노출하는 optimizer를 위해 sampled-parameter training branch를 trainer에 추가했습니다.
- benchmark 해석 메모:
  - HBTXR trainer의 sampled-parameter path는 현재 `amp` / `GradScaler`를 지원하지 않습니다.
  - validation은 posterior averaging이 아니라 posterior-mean weight 기준으로 수행됩니다.
