# Stage2 track-only fine-tune with decomposed ellipse-state supervision.
# This keeps checkpoint compatibility: no new head parameters, only decoded-state
# axis/angle losses motivated by FACET-style ellipse tracking.

extends: mode1_stage2_raw_event_count_lr5e-6_nodistill_trackonly_centerloss_adaptivecount_finetune_fullwidth.yaml

loss:
  track_axis_log_weight: 0.05
  track_angle_cos_weight: 0.02

experiment:
  name: raw_mode1_stage2_lr5e-6_nodistill_trackonly_ellipsestate_finetune_fullwidth
