# Stage2 fine-tune: conservative track-head-only sibling of the LR 1e-5
# no-distillation run.

extends: mode1_stage2_raw_event_count_lr1e-5_nodistill_trackonly_centerloss_finetune_fullwidth.yaml

training:
  lr: 5.0e-6
  optimizer:
    name: adamw
    lr: 5.0e-6
    weight_decay: 5.0e-5
    betas: [0.9, 0.999]
    eps: 1.0e-8
    kwargs: {}

experiment:
  name: raw_mode1_stage2_lr5e-6_nodistill_trackonly_centerloss_finetune_fullwidth
