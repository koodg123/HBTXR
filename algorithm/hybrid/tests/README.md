# Hybrid Tests

Active tests in this directory should validate the current hybrid package.

- `external_pipeline/`: tests aligned with the maintained hybrid external
  pipeline surface.

Historical reference tests are archived under
`algorithm/archive/imports/legacy_hybrid/tests/` until they are ported.
