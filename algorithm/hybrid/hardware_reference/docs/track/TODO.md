# HGTXR-SW TODO

- P0: Experiments are paused by user directive on 2026-06-18. Do not launch train/eval jobs until the user explicitly resumes execution.
- P0: Current pause/current-state documentation is recorded in `docs/resources/experiment_pause_documentation_2026_06_18.md`.
- P0: Second-goal completion audit is recorded in `docs/resources/second_goal_completion_audit_2026_06_18.md`; active goal remains incomplete until XR-64 execution and final accuracy closure are verified.
- P0: Second-goal artifact index is recorded in `docs/resources/second_goal_artifact_index_2026_06_18.md` and `.json`; use it as the navigation entry point for analysis, plans, validation, and resume tools.
- P0: Use `.venv/bin/python scripts/external/report_second_goal_status.py --format summary` for read-only current-state reporting while experiments remain paused.
- P0: Software cleanup is in inventory-only mode. Use `docs/resources/software_cleanup_refactor_plan_2026_06_18.md`, `software_cleanup_inventory_2026_06_18.md`, and `software_path_reference_audit_2026_06_18.md` before any delete/move action.
- P0: `runs/` has been physically organized under `runs/XR-<n>/...` and `runs/NON_XR/...`; top-level `eval*`, `raw*`, and `xr*` compatibility symlinks were removed on 2026-06-18 after active path discovery/checkers were migrated.
- P0: Only shared top-level aliases remain in `runs/`: `_logs`, `diagnostics`, and `interpolated_checkpoints`; keep them until all legacy scripts using these shared paths are migrated to `runs/NON_XR/shared/...`.
- P0: Before deleting or archiving any `runs/` directory, create a preserve manifest covering XR-39/XR-56/XR-58/XR-62/XR-63/XR-64, `interpolated_checkpoints`, `_logs`, and `diagnostics`.
- P1: Normalize active script path handling (`PYTHON_BIN`, `CANONICAL_ROOT`, `PATHS_CONFIG`) before moving `scripts/external/run_xr*.sh`.
- P0: XR-64 resume commands and validation gates are recorded in `docs/resources/xr64_resume_runbook_2026_06_18.md`; use this as the execution checklist when experiments resume.
- P0: Before XR-64A/B execution after resume, run `.venv/bin/python scripts/external/check_xr64_resume_artifacts.py`; it must return `0`.
- P0: Current consolidated status and next experiment list are recorded in `docs/resources/current_work_summary_and_next_experiments_2026_06_18.md`.
- P0: Current PAPER_REF-to-experiment ablation matrix is recorded in `docs/resources/second_goal_paper_ref_current_ablation_plan_2026_06_20.md`. Treat this as the current bridge from PAPER_REF evidence to XR-64/XR-65/XR-66/XR-67/XR-68 priorities.
- P0: PAPER_REF experiment trace is recorded in `docs/resources/second_goal_paper_ref_experiment_trace_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_second_goal_paper_ref_experiment_trace.py --format summary` before opening/reprioritizing any PAPER_REF-derived experiment; it must preserve the XR-64-first queue, six paper evidence groups, eight experiment traces, no-execute validation commands, and current blockers.
- P0: Current machine-readable experiment queue is recorded in `docs/resources/second_goal_experiment_queue_2026_06_20.json` with human summary `docs/resources/second_goal_experiment_queue_2026_06_20.md`. Run `.venv/bin/python scripts/external/check_second_goal_experiment_queue.py --format summary` before treating the queue as executable.
- P0: Second-goal ablation evidence checklist is recorded in `docs/resources/second_goal_ablation_evidence_checklist_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_second_goal_ablation_evidence_checklist.py --format summary` before using planned XR-64/XR-65/XR-66/XR-67/XR-68 experiments as ablation evidence. It is a no-execute evidence contract; current software promotion remains blocked.
- P0: Second-goal completion readiness contract is recorded in `docs/resources/second_goal_completion_readiness_contract_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_second_goal_completion_readiness_contract.py --format summary` before any completion/readiness claim. It must remain aligned with `report_second_goal_status.py` and `check_second_goal_completion_gate.py`.
- P0: Submission-target gap audit is recorded in `docs/resources/second_goal_submission_target_gap_audit_2026_06_20.md` and `.json`. Run `.venv/bin/python scripts/external/check_submission_target_gap.py --format summary` before using it in a completion claim. Do not claim progress toward the paper's `0.1812 px` target until the coordinate/protocol frame is matched; use current center/P10/P5 gates for software promotion meanwhile.
- P0: Submission-target source trace is recorded in `docs/resources/submission_target_source_trace_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_submission_target_source_trace.py --format summary` before citing paper target values. The checker scopes HBTXR algorithm values to `tab:algo` and enforces that accelerator table ranges are not treated as a single software accuracy target.
- P0: Metric/protocol bridge is recorded in `docs/resources/second_goal_metric_protocol_bridge_2026_06_20.md` and `.json`. Run `.venv/bin/python scripts/external/check_metric_protocol_bridge.py --format summary` before any final accuracy-closure claim. Current bridge status is blocked because metrics are post-transform track metrics, paper-frame/full-test P1 evidence is absent, and hybrid scheduler-mode evaluation is not proven.
- P0: Metric/protocol unblock contract is recorded in `docs/resources/metric_protocol_unblock_contract_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_metric_protocol_unblock_contract.py --format summary` before any direct paper-target comparison. Current contract status is `blocked`; required evidence includes coordinate-frame match, hybrid scheduler eval, P1 metric coverage, split protocol match, target definition match, latency separation, and trained XR-64-or-later result dependency.
- P0: Paper-target comparison evidence manifest is recorded in `docs/resources/paper_target_comparison_evidence_manifest_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_paper_target_comparison_evidence_manifest.py --format summary` before claiming the metric/protocol bridge has concrete evidence. Current state is `missing_required_evidence` with 10 future required artifacts, 10 future artifact content schemas, and 3 existing doc-prefill bridge artifacts; direct paper-target comparison remains blocked.
- P0: Paper-target bridge artifact workplan is recorded in `docs/resources/paper_target_bridge_artifact_workplan_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_paper_target_bridge_artifact_workplan.py --format summary` before creating bridge artifacts. While experiments are paused, only `split_protocol_audit`, `cur_state_mapping_audit`, and `accuracy_latency_separation_note` are safe doc-prefill artifacts; sensor-space rows, hybrid eval rows, and trained-candidate artifacts remain blocked until explicit resume/evaluation.
- P0: Paper-target bridge evaluator schema contract is recorded in `docs/resources/paper_target_bridge_evaluator_schema_contract_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_paper_target_bridge_evaluator_schema_contract.py --format summary` before implementing or generating PTB-1 evaluator outputs. Current PTB-1 output payload count must stay `0` while experiments are paused.
- P0: Paper-target bridge PTB-1 resume runbook is recorded in `docs/resources/paper_target_bridge_ptb1_resume_runbook_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_paper_target_bridge_ptb1_resume_runbook.py --format summary` and `.venv/bin/python scripts/external/check_paper_target_bridge_payloads.py --allow-present --require-all-present --format summary` after explicit resume and payload generation. While paused, the five official PTB-1 output payloads must remain absent.
- P0: Paper-target bridge resume dependency matrix is recorded in `docs/resources/paper_target_bridge_resume_dependency_matrix_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_paper_target_bridge_resume_dependency_matrix.py --format summary` before any PTB-3 bridge decision or direct paper-target comparison claim. Current matrix state must remain `complete_now_gate_count=2`, `blocked_gate_count=4`, and `execution_dependent_payload_missing_count=8` while experiments are paused.
- P0: P1 evaluator implementation is now available as `metric_track_p1_pct` and `metric_search_p1_pct`. This does not unblock paper comparison; still require paper-frame/full-test P1 evidence, sensor-space coordinate matching, and hybrid scheduler rows.
- P0: Current result synthesis is recorded in `docs/resources/second_goal_current_result_synthesis_2026_06_20.md` and `.json`. Run `.venv/bin/python scripts/external/check_current_result_synthesis.py --format summary` before summarizing current best results. It enforces that XR-63 is diagnostic only, no single-model best claim is allowed, direct paper-target comparison remains blocked, and XR-64 is still `generated_incomplete`.
- P0: Second-goal objective trace is recorded in `docs/resources/second_goal_objective_trace_2026_06_20.md` and `.json`. Run `.venv/bin/python scripts/external/check_second_goal_objective_trace.py --format summary` before any completion claim. It enforces REQ-1 `complete_as_planning_input`, REQ-2 `planned_not_fully_executed`, REQ-3 `incomplete`, all requested experiment axes covered, full 8-experiment ablation matrix coverage, and `goal_complete=false`.
- P0: Second-goal completion gate is available at `scripts/external/check_second_goal_completion_gate.py`. Run `.venv/bin/python scripts/external/check_second_goal_completion_gate.py --allow-incomplete --format summary` for current blocker reporting, and require strict completion success before marking the active goal complete. Current state reports `completion_allowed=false` with `28` blockers, including paper-target evidence manifest blockers and XR-64 post-run promotion evidence blockers.
- P0: XR-64 resume command manifest is recorded in `docs/resources/xr64_resume_command_manifest_2026_06_20.md` and `.json`. Run `.venv/bin/python scripts/external/check_xr64_resume_command_manifest.py --format summary` before using future XR-64-prep commands. It enforces `10` future prep commands, `8` eval outputs, `6` override outputs, train/val-only splits, no test override command, strict readiness before XR-64A/B, `2` post-run candidate/decision-review commands, `allowed_to_run=false` while paused, and eval/build required input readiness. Current eval static inputs are `6/6` ready; current build inputs are `0/8`, so build steps are not ready.
- P0: XR-64 resume command emitter is available at `scripts/external/emit_xr64_resume_commands.py`. Use `.venv/bin/python scripts/external/emit_xr64_resume_commands.py --section summary --format summary` for counts, `--section prep --format commands` to print future prep commands for review, and `--section postrun --format commands` to print candidate collection/decision-review commands after future XR-64A/B eval summaries exist. It does not execute commands and reports `execute_supported=false`.
- P0: XR-64 prelaunch packet is recorded in `docs/resources/xr64_prelaunch_packet_2026_06_20.md` and `.json`. Run `.venv/bin/python scripts/external/check_xr64_prelaunch_packet.py --format summary` before resuming XR-64 work. It ties the command manifest, completion gate, strict readiness, eval/build input readiness, post-run candidate review, and current blockers into one future-only packet. Current state is `allowed_to_execute=false`, eval static inputs `6/6` ready, build inputs `0/8` ready, and post-run command count `2` while paused.
- P0: XR-64 post-run evidence contract is recorded in `docs/resources/xr64_postrun_evidence_contract_2026_06_21.md` and `.json`. Run `.venv/bin/python scripts/external/check_xr64_postrun_evidence_contract.py --format summary` before using future XR-64A/B results for promotion. It requires trained checkpoint evidence, override-cleared test eval summaries/rows, lane provenance, `metric_track_p1_pct` as paper-target bridge evidence, and tradeoff reporting against center/P10/P5 gates.
- P0: XR-64 future runs now call `scripts/external/write_xr64_ablation_provenance.py` from `scripts/external/run_xr64_teacher_target_construction.sh`. After explicit resume and real XR-64A/B execution, verify each run root has `train/ablation_lr_manifest.json`, `train/optimizer_config_snapshot.json`, `train/teacher_provenance.json`, and `train/ablation_attribution_report.json`; verify each test `eval_summary.json` has strict ablation axis certification before using `decide_xr64_postrun_promotion.py`.
- P0: XR-64 runner defaults to `XR64_REQUIRE_ALL_CHECKPOINTS=1`; missing `best_track_p10`, `best_track_p5`, `best_metric_track_center_px`, test `eval_summary.json`, or test `eval_rows.json` must fail the future run. Use `XR64_REQUIRE_ALL_CHECKPOINTS=0` only for explicitly labeled diagnostics that cannot be promoted.
- P0: After future XR-64A/B eval summaries exist, run `.venv/bin/python scripts/external/collect_xr64_postrun_candidates.py --format summary` to discover certified candidates before manually assembling promotion commands. Use `--format commands` to print the exact `decide_xr64_postrun_promotion.py --candidate ...` invocation.
- P0: XR-64 post-run promotion decision helper is available at `scripts/external/decide_xr64_postrun_promotion.py`. After future XR-64A/B test eval summaries exist, pass candidates as `--candidate XR-64A:best_track_p10:path/to/eval_summary.json` and require the helper result before updating any software leader gate. The helper tracks `metric_track_p1_pct` as future paper evidence but keeps software promotion on center/P10/P5.
- P0: PAPER_REF per-paper traceability is recorded in `anlaysis/paper-ref/papers/index.md` and 31 `papers/*/analysis.md` files. Use these before opening a new paper-derived experiment.
- P0: Before adding or reopening any PAPER_REF-derived experiment, run `.venv/bin/python scripts/external/check_paper_ref_analysis_coverage.py --format summary`; it must report `ok: true`.
- P0: Historical P0 rows that name XR-15, XR-60, candidate-head-only, geometry-only replay, scalar P10-soft continuation, low-similarity weighting, or blind local-crop continuation are superseded by the XR-64-first plan unless explicitly reopened with a new mechanism and gate.
- P0: XR-64 remains the immediate execution priority after experiments resume. Use `bash scripts/external/run_xr64_teacher_eval_rows_and_overrides.sh` to generate train/val teacher eval rows and train/val-only override JSONs. The first all-in-one attempt stopped without eval rows, but an 8-row direct XR-62A smoke eval succeeded, so rerun with split/teacher-level logging if needed. Do not run while the pause directive is active.
- P0: XR-64A conservative teacher-target construction after override files exist: `bash scripts/external/run_xr64_teacher_target_construction.sh a cuda:0`. Target gates: center `<16.468481131962367`, P10 `>35.02295998845781`, P5 `>12.133503770828247`.
- P0: XR-64B threshold-priority teacher-target construction after override files exist: `bash scripts/external/run_xr64_teacher_target_construction.sh b cuda:1`. Treat as P10/P5 recovery attempt; reject promotion if center/P5 collapse is not documented as diagnostic.
- P1: XR-64C min-error teacher-target diagnostic only after XR-64A/B or if A/B fail to move validation. Use `bash scripts/external/run_xr64_teacher_target_construction.sh c cuda:0`; do not use test-derived override targets.
- P1: XR-65 teacher-target temporal-lite residual after XR-64. Candidate knobs: XR-64 best init/teacher, AdamW `2e-7~3e-7`, state distill `0.00020~0.00035`, train only `event_adapter.*`, `patch_frontend.event_embed.proj.*`, and selected heads. Stop if val center worsens by `>0.3 px`.
- P1: XR-66 EX-Gaze confidence-gated local update diagnostic after XR-64. First produce no-train confidence buckets; do not train local crop/update without a fallback gate.
- P2: XR-67 EyeLoRiN/dense-trajectory reopening only if dense or continuous trajectories exist. Current manifest has no dense segment under `50000us`, so this remains blocked as primary work.
- P2: XR-68 sparse/quant/distillation compression branch after a stable full-width teacher exists. Do not start hardware/latency branch before software accuracy gate improves.
- P0: XR-64 code path is implemented. Next concrete action: generate train eval rows for XR-62A/XR-39/XR-56B/XR-58A, build train-only override files with `scripts/external/build_xr64_teacher_target_overrides.py`, then launch `bash scripts/external/run_xr64_teacher_target_construction.sh a cuda:0` and lane B on `cuda:1`.
- P0: XR-63 no-train teacher-target oracle is complete. Oracle upper bound across XR-62A/XR-39/XR-56B/XR-58A reaches `16.04023192701366/36.48596938775512/13.41751700680271`, exceeding all active gates.
- Historical: XR-60 P10 candidate-head calibration was an immediate next experiment before XR-61/XR-62/XR-63/XR-64 evidence. XR-60/XR-61 candidate-head-only continuation is now low-return unless it is paired with teacher-target selection.
- P0: XR-59 narrow anchored-teacher bracket is closed with no promotion. Best XR-59 P10 was `34.43409944261823`, worse than XR-58A `34.90306201662336` and XR-39 `35.02295998845781`. Close scalar P10-soft teacher continuation; next P0 must change representation or protocol, such as a dedicated P10 calibration/classification head, subject/session failure-bucket data diagnostics, or different teacher target construction.
- P0: XR-55 expanded last-block P10 branch is closed with no promotion. Best P10 was `34.793368141992`, still below XR-39 `35.02295998845781`, and center/P5 regressed. Direct supervision is now being tested by XR-56.
- P0: XR-53 no-train recombination is closed with no promotion. Best P10 was only `34.623300109591`, so skip XR-54 near-tie continuation and move to XR-55 XR39-anchored expanded-scope P10 model branch.
- P0: Current active gates after XR-52 are center `<16.470460832119` from XR-52A best-P5, P10 `>35.02295998845781` from XR-39 `c25p45f30`, and P5 `>12.017432342257` from XR-52B best-P10. Immediate next action is P10-focused XR-53 checkpoint-space recombination with XR-52 center/P5 leaders and XR-39 P10, or a stronger P10 teacher/model branch.
- P0: Current active gates after XR-51 are center `<16.478984827655` from XR-51B `t60c20p20`, P10 `>35.02295998845781` from XR-39 `c25p45f30`, and P5 `>11.978316681725639` from XR-50B best-P10 / XR-51A `a0p05`. Immediate next action is XR-52 trainable P10 recovery with explicit P5 preservation; do not spend more primary queue on pure no-train P10 soups unless used as a bounded diagnostic.
- P0: Track mixed active gates after XR-33: center leader is XR-30 support-adaptive fixed255k AdamW LR `1.875e-4` with center `17.0355`; P10 leader is XR-33 LR `1.84375e-4` with P10 `32.6207`; P5 leader remains XR-29 LR `1.75e-4` with P5 `11.5047`. Best unified checkpoint remains XR-29 LR `1.75e-4`.
- P0: Retain XR-20A/XR-06C/XR-22 only as historical anchors: XR-20A center `20.1755`, XR-06C P10 `26.7449`, XR-22 P5 `8.8690`.
- P0: XR-07A/XR-07B tiny center-hinge recovery is closed as no-promotion on validation metrics; do not continue hinge weight escalation from this branch without a new diagnostic.
- P0: XR-06A closeout is complete. Best-center promoted center to `20.2838`; best-P10 did not promote.
- P0: XR-06B closeout is complete. Best-center `20.2950/26.2177/8.5191` and best-P10 `20.4695/25.9320/8.6310`; no promotion.
- P0: XR-05F no-distill control is complete. Best-center `20.3562/26.4328/8.2980` and best-P10 `20.3195/26.1509/8.1429`; no promotion.
- P0: XR-06C/XR-05G closeout is complete. XR-05G did not promote; XR-06C promoted center and P10.
- P0: XR-06D/XR-06E closeout is complete. Neither promoted; XR-06D reached `20.2956/26.1586/8.5047`, XR-06E best-center reached `20.2942/26.2806/8.5387`, and XR-06E best-P10 reached `20.3149/26.1339/8.6811`.
- P0: XR-08 checkpoint interpolation smoke is complete. Alpha `0.50` between XR-06C center/P10 leaders reached `20.3259/26.4983/8.3865`; no promotion. Keep scripts for later, but move primary queue to XR-05I.
- P0: XR-05I P5-preserve direct-aux fallback is complete. Best-center `20.3510/26.1501/8.4290`; best-P10 `20.3939/26.1701/8.4503`; no promotion.
- P0: XR-06F complementary P5-preserve weak-distill fallback is complete. Best-center `20.3222/26.2007/8.6259`; best-P10 `20.4218/26.3946/8.6301`; no promotion.
- P0: XR-04B/XR-04C focused low-similarity branches are complete and did not promote. XR-04B best-center/best-P10 reached `20.4841/25.6003/8.2776` and `20.6738/25.4014/7.8754`; XR-04C best-center/best-P10 both reached `20.5883/25.6582/7.8499`.
- P0: XR-04D/XR-04E manifest-subset failure-bucket experiments are complete and did not promote. XR-04D LR `6e-6` best-center/best-P10 reached `24.5081/16.7526/4.5179` and `25.7039/15.3074/4.2381`; XR-04E LR `3e-6` best-center/best-P10 reached `23.5577/18.1594/5.1063` and `23.4280/18.2555/5.0310`.
- P0: Do not repeat global low-similarity weighting, hard failure-bucket subset training, or local low-LR polish. Next options are weighted sampler/full-manifest balancing, broader subset only as diagnostic analysis, or XR-02 only after dense trajectories are available.
- P0: XR-09A/XR-09B full-manifest weighted-sampler probes are closed with no promotion. Do not repeat sampler-only weighting unless it tests a distinct hypothesis.
- P0: XR-10A/XR-10B full-manifest loss-side sample weighting is closed with no promotion. Do not repeat low-sim/session_201 rebalancing unless it changes model representation or data density.
- P0: Retain XR-01 promoted leader as demoted baseline: fixed255k AdamW LR `1e-5` best-center, center `26.1749`, P10 `16.5021`, P5 `5.0999`.
- P0: XR-02 EyeLoRiN-style post-process is implemented, but current canonical1 labels have no dense segment under `50000us` (`dense_pair_count=0`, longest segment `1`). Reopen only if dense labels, canonical2, or a continuous split exists.
- P0: XR-04 low-similarity loss did not promote. XR-05 direct state aux did promote. XR-05D failed, XR-05E promoted only P10; XR-07 failed validation. XR-06A promoted center; XR-06B/XR-05F failed to promote; XR-05G failed; XR-06C promoted center and P10; XR-06D/XR-06E and XR-08 failed to promote. XR-04 refresh confirms low-sim/session/subject failure concentration. Confidence probe rejects no-train confidence gating from current logits.
- P0: XR-14A all-head relocalization is closed with no promotion. Do not train calibrated search/event fallback from this branch; `search_state`/`event_state` scored P10/P5 `0.0`. XR-15A/B/C support-adaptive fixed-count event-window training promoted center, XR-15C P10-lane best-P5 evaluation promoted P5, XR-17A alpha `0.75` promoted P5, XR-17B promoted center/P5, XR-18A interpolation failed, XR-19A micro P10 recovery promoted center only, and XR-20A LR `2.5e-7` promoted center again. XR-20B LR `5e-7` failed. XR-21 P10-margin mechanism-change probes failed to promote. XR-22 tri-leader checkpoint soup promoted P5 to `8.8690`. XR-23 ADOPT/Lion P10-preserving optimizer probe failed to promote. XR-24 XR-22-anchor AdamW P10 recovery failed. XR-25 XR-22-anchor ADOPT-defaults also failed to promote, though best-P5 reached `8.8661`. XR-26 P10-boundary head-only loss also failed to promote. XR-27 track-heatmap coordinate representation promoted all active gates to center `17.2746`, P10 `31.9154`, P5 `10.2900`. XR-28 diagnostics confirmed low/mid-similarity bucket gains, and XR-28 LR `1.5e-4` refinement promoted all active gates to center `17.0849`, P10 `32.0812`, P5 `10.5026`. XR-29 LR `1.75e-4` promoted all gates again to center `17.0496`, P10 `32.5646`, P5 `11.5047`. XR-30 LR `1.875e-4` promoted center only to `17.0355`. XR-31 alpha `0.25` interpolation promoted P10 to `32.5795` but missed P5 by about `0.00085`. XR-32 LR `1.8125e-4` did not promote. XR-33 LR `1.84375e-4` promoted P10 to `32.6207` but regressed center/P5. Current immediate action is heatmap loss-ratio refinement or bounded XR-29/XR-33 anchor/soup regularization; defer close LR-only replay unless history shows non-convergence.
- P1: Use completed XR-04 failure-bucket and confidence diagnostics as evidence; artifacts: `runs/diagnostics/xr04_failure_buckets_adamw255k_leader_test_20260616.json`, `runs/diagnostics/xr04_refresh_xr06c_bestcenter_failure_buckets_20260616.json`, `runs/diagnostics/xr04_refresh_xr06c_bestp10_failure_buckets_20260616.json`, `runs/diagnostics/xr04_confidence_probe_low0p1_high0p6_128_xr06c_bestcenter_buckets_20260616.json`, and `runs/diagnostics/xr04_confidence_probe_low0p1_high0p6_128_xr06c_bestp10_buckets_20260616.json`.
- P1: Queue XR-05 small temporal head or heatmap/KL auxiliary only after XR-04 diagnostics identify a targetable failure mode.
- P2: Defer XR-06 sparse/quant/distillation edge path until full-width software teacher stabilizes.
- P2: Defer XR-07 synthetic/unlabeled pretraining until data scarcity or subject overfit is confirmed.
- Verify metric coordinate frame against paper-reported pixel error.
- Generate sample overlays: frame, event, mask, cur_state, prev_state, track target.
- Run `lr2e-4_nodistill_fullwidth` Stage2 full ablation.
- Run event-count sweep: `1000`, `2500`, `10000`.
- Add test or checker for history regression summary.
- Add mode2 dense supervision recovery plan after raw ablations.
